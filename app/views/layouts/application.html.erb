<!DOCTYPE html>
<html>
<head>
  <title>Iceman</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
<style>
      body {
        padding-top: 60px; /* 60px to make the container go all the way to the bottom of the topbar */
      }
    </style>

<div class="navbar navbar-inverse navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container">
          <button type="button" class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="brand" href="/">Migration</a>
          <div class="nav-collapse collapse">
            <ul class="nav">
              <% if current_user %>
              <li class="active"><a href="/"><%= current_user.name %> </a></li>
              
                  <li> <img src=<%=current_user.image%>><li> </div>
                  
                  <li><div class="container"><%= link_to "Sign out", signout_path, id: "sign_out" %><li></div>
              <% else %>
                <li> <%= link_to "Sign in with Facebook", "/auth/facebook", id: "sign_in" %> </li>
               <% end %>       
            </ul>
          </div><!--/.nav-collapse -->
        </div>
      </div>
    </div>


<%= yield %>

</body>
</html>
