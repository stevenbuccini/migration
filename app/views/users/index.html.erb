
<h1>Users#index</h1>
<p>Find me in app/views/users/index.html.erb</p>

<% if current_user %>
    Signed in as <strong><%= current_user.name %></strong> from <strong><%= current_user.location%></strong> that looks like <img src=<%=current_user.image%>> !

    <%= link_to "Sign out", signout_path, id: "sign_out" %>
  <% else %>
    <%= link_to "Sign in with Facebook", "/auth/facebook", id: "sign_in" %>
  <% end %>